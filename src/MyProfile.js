import {
  Text,
  View,
  TextInput,
  Alert,
  TouchableOpacity,
  Button,
} from "react-native";
import { styles } from "./Styles";

export default function MyProfile(props) {
  return (
    <View style={styles.container}>
      <Text style={{ color: "#fff" }}>Dummy profile</Text>
    </View>
  );
}
